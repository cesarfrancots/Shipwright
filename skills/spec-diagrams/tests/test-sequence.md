# Test: payment retry sequence

## Input

Use the `spec-diagrams` skill.

Create a sequence diagram for a failed subscription renewal that triggers a retry policy, sends an email, and updates the billing dashboard.

## Expected Behavior

- [ ] Output uses a sequence diagram.
- [ ] Actors and systems are named explicitly.
- [ ] Diagram is valid Mermaid syntax.
- [ ] Notes mention any assumed systems or missing details.
